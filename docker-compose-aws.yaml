services:
  app:
    container_name: bookora_aws_compose
    image: ajayrohith/bookora_aws_repo:latest
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=aws
      - DB_PASSWORD=${DB_PASSWORD}
    
